struct Transaction {
    u32 sourceIndex;
    u32 targetIndex;	
	u32 amount;
    u32 nonce;
}

struct Signature {
    field[2] r;
    field s;	
	field[2] a;
}

struct TransactionHelper {
	u32[8] sourceAddress;
    u32[8] targetAddress; 
    Signature signature;  
}

